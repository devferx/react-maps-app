import { MapView, BtnMyLocation, ReactLogo, SearchBar } from "../components";

export const HomePage = () => {
  return (
    <div>
      <MapView />
      <BtnMyLocation />
      <ReactLogo />
      <SearchBar />
    </div>
  );
};
